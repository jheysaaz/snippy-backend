# PostgreSQL Configuration
DATABASE_URL=postgres://postgres:postgres@localhost:5432/snippy?sslmode=disable

# Server Configuration
PORT=8080

# JWT Configuration (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-secret-key-change-in-production-use-a-long-random-string

# CORS Configuration (comma-separated origins for production)
# Development: * (allow all)
# Production: https://yourdomain.com,https://extension-id.chromiumapp.org
CORS_ALLOWED_ORIGINS=*
