# =============================================================================
# Snippy API Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
POSTGRES_USER=snippy_user
POSTGRES_PASSWORD=change_this_password
POSTGRES_DB=snippy_production
DATABASE_URL=postgres://snippy_user:change_this_password@postgres:5432/snippy_production?sslmode=disable

# -----------------------------------------------------------------------------
# API Server
# -----------------------------------------------------------------------------
GIN_MODE=release

# JWT secret (MUST change in production - use: openssl rand -base64 32)
JWT_SECRET=your-secret-key-change-in-production

# CORS (comma-separated origins)
CORS_ALLOWED_ORIGINS=https://yourdomain.com

# -----------------------------------------------------------------------------
# SSL / Let's Encrypt (Production only)
# -----------------------------------------------------------------------------
# Your domain name (leave empty or "localhost" for local development)
DOMAIN=

# Email for Let's Encrypt certificate expiration notices
CERTBOT_EMAIL=

# Set to "true" to use Let's Encrypt staging server (for testing)
CERTBOT_STAGING=false

